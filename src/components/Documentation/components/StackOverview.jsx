
import React from "react";

const StackOverview = () => (
  <section>
    <h2>Stack Data Structure</h2>
    <p>A stack is a linear data structure that follows the Last In, First Out (LIFO) principle.</p>
    <p>Common operations include:</p>
    <ul>
      <li><b>Push:</b> Adds an item to the stack.</li>
      <li><b>Pop:</b> Removes the top item from the stack.</li>
      <li><b>Peek:</b> Views the top item without removing it.</li>
      <li><b>isEmpty:</b> Checks if the stack is empty.</li>
    </ul>
  </section>
);

export default StackOverview;
